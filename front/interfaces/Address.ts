/**
 * The Address interface contains data
 * about a post code.
 */
export interface IAddress {
  country: string;
  district: string;
  lat: number;
  long: number;
  postcode: string;
  zone: string;
}
